# Set the name of your Spring Boot application (Optional)
spring.application.name=ocampog4

# ===============================================
# CRITICAL DATABASE CONFIGURATION
# ===============================================

# 1. Connection URL: Points to the 'dbappdevg4asyncoampo' database we created
spring.datasource.url=jdbc:mysql://localhost:3306/dbappdevg4asyncocampo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 2. Login Credentials: YOU MUST REPLACE THESE
# Try 'root' as the username first, and then enter your password.
spring.datasource.username=root
spring.datasource.password=#a12itech

# ===============================================
# JPA/Hibernate Configuration
# 'update' automatically creates and updates tables based on your Java Entities
# ===============================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Set the timezone explicitly to avoid warnings
spring.jpa.properties.hibernate.jdbc.time_zone=UTC